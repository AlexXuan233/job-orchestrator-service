apiVersion: v1
kind: Service
metadata:
  name: job-orchestrator
  namespace: default
  labels:
    app: job-orchestrator
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: job-orchestrator
